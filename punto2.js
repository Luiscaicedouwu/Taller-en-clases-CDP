// Punto numero 2 //

function es_divisor(numero) {
    if(numero % 2 === 0 ){
        console.log("El numero " + numero + " es divisor" + 2)
        
    }
    else console.log("El numero " + numero + " no es divisor" + 2)

}

es_divisor(5)
